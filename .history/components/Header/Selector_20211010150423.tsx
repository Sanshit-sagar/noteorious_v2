


const MenuBarSelector = ({ icon, title, action, isActive = false, isDisabled = false, isHovered , type = 'select', selectProps }) => {
    return (
        <MenuBarItem>
            hihi 
        </MenuBarItem>
    );
}