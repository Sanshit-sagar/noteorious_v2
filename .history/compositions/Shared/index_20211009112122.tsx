import { styled } from '../../stitches.config'

export const Wrapper = () => {
    display: 'inline-flex',
    flexDirection: 'column', 
    position: 'relative',
    marginTop: '20px'
};

export const Label = () => {
    display: 'block',
    textAlign: 'left',
    fontSize: '2'
}